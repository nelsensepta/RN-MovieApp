import { StatusBar } from "expo-status-bar";
import React from "react";
import { StyleSheet, Text, View } from "react-native";
import NavStack from "./route/NavStack";

export default function App() {
  return <NavStack />;
}

const styles = StyleSheet.create({});
